---
title: NuGet.exe Anmeldeinformationsanbieter
description: Anmeldeinformationsanbieter NuGet.exe authentifizieren sich mit einem Feed und werden als ausführbare Befehlszeilendateien, die bestimmte Konventionen implementiert.
author: kraigb
ms.author: kraigb
manager: douge
ms.date: 12/12/2017
ms.topic: conceptual
ms.openlocfilehash: 494ea83007895e973585395e0cfe05b7226c4c3e
ms.sourcegitcommit: 3eab9c4dd41ea7ccd2c28bb5ab16f6fbbec13708
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 04/26/2018
---
# <a name="authenticating-feeds-with-nugetexe-credential-providers"></a><span data-ttu-id="c0cba-103">Authentifizieren von Feeds mit nuget.exe Anmeldeinformationsanbieter</span><span class="sxs-lookup"><span data-stu-id="c0cba-103">Authenticating feeds with nuget.exe credential providers</span></span>

<span data-ttu-id="c0cba-104">*NuGet 3.3+*</span><span class="sxs-lookup"><span data-stu-id="c0cba-104">*NuGet 3.3+*</span></span>

<span data-ttu-id="c0cba-105">Wenn `nuget.exe` benötigt Anmeldeinformationen für die Authentifizierung bei einem Feed, sucht es nach ihnen auf folgende Weise:</span><span class="sxs-lookup"><span data-stu-id="c0cba-105">When `nuget.exe` needs credentials to authenticate with a feed, it looks for them in the following manner:</span></span>

1. <span data-ttu-id="c0cba-106">NuGet sucht zuerst nach Anmeldeinformationen in `Nuget.Config` Dateien.</span><span class="sxs-lookup"><span data-stu-id="c0cba-106">NuGet first looks for credentials in `Nuget.Config` files.</span></span>
1. <span data-ttu-id="c0cba-107">NuGet verwendet-Plug-In für anmeldeinformationenanbieter, unterliegen der unten angegebenen Reihenfolge.</span><span class="sxs-lookup"><span data-stu-id="c0cba-107">NuGet then uses plug-in credential providers, subject to the order given below.</span></span> <span data-ttu-id="c0cba-108">(Und Beispiel ist die [Visual Studio Team Services-Anmeldeinformationsanbieter](https://www.visualstudio.com/docs/package/get-started/nuget/auth#vsts-credential-provider).)</span><span class="sxs-lookup"><span data-stu-id="c0cba-108">(And example is the [Visual Studio Team Services Credential Provider](https://www.visualstudio.com/docs/package/get-started/nuget/auth#vsts-credential-provider).)</span></span>
1. <span data-ttu-id="c0cba-109">NuGet fordert dann den Benutzer zum Eingeben von Anmeldeinformationen in der Befehlszeile angegeben.</span><span class="sxs-lookup"><span data-stu-id="c0cba-109">NuGet then prompts the user for credentials on the command line.</span></span>

<span data-ttu-id="c0cba-110">Beachten Sie, die nur in der hier beschriebenen Anmeldeinformationsanbieter funktionieren `nuget.exe` und nicht in "Dotnet wiederherstellen" oder im Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="c0cba-110">Note that the credential providers described here work only in `nuget.exe` and not in 'dotnet restore' or Visual Studio.</span></span> <span data-ttu-id="c0cba-111">Der Anmeldeinformationsanbieter mit Visual Studio, finden Sie unter [nuget.exe Anmeldeinformationsanbieter für Visual Studio](nuget-credential-providers-for-visual-studio.md)</span><span class="sxs-lookup"><span data-stu-id="c0cba-111">For credential providers with Visual Studio, see [nuget.exe Credential Providers for Visual Studio](nuget-credential-providers-for-visual-studio.md)</span></span>

<span data-ttu-id="c0cba-112">Anmeldeinformationsanbieter NuGet.exe können auf 3 Arten verwendet werden:</span><span class="sxs-lookup"><span data-stu-id="c0cba-112">nuget.exe credential providers can be used in 3 ways:</span></span>

- <span data-ttu-id="c0cba-113">**Global**: einen Anmeldeinformationsanbieter für alle Instanzen von verfügbar machen können `nuget.exe` unter Profil des aktuellen Benutzers ausgeführt werden, fügen Sie diese `%LocalAppData%\NuGet\CredentialProviders`.</span><span class="sxs-lookup"><span data-stu-id="c0cba-113">**Globally**: to make a credential provider available to all instances of `nuget.exe` run under the current user's profile, add it to `%LocalAppData%\NuGet\CredentialProviders`.</span></span> <span data-ttu-id="c0cba-114">Möglicherweise müssen Sie erstellen die `CredentialProviders` Ordner.</span><span class="sxs-lookup"><span data-stu-id="c0cba-114">You may need to create the `CredentialProviders` folder.</span></span> <span data-ttu-id="c0cba-115">Anmeldeinformationsanbieter können installiert werden, im Stammverzeichnis der `CredentialProviders` Ordner oder in einem Unterordner.</span><span class="sxs-lookup"><span data-stu-id="c0cba-115">Credential providers can be installed at the root of the `CredentialProviders`  folder or within a subfolder.</span></span> <span data-ttu-id="c0cba-116">Verfügt ein Anmeldeinformationsanbieter mehrere Dateien/Assemblys, können Sie die Unterordner organisiert Anbieter zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="c0cba-116">If a credential provider has multiple files/assemblies, you can use subfolders to keep the providers organized.</span></span>

- <span data-ttu-id="c0cba-117">**Von einer Umgebungsvariablen**: Anmeldeinformationsanbieter an einer beliebigen Stelle gespeichert und verfügbar gemacht werden `nuget.exe` durch Festlegen der `%NUGET_CREDENTIALPROVIDERS_PATH%` -Umgebungsvariable für den Speicherort eines Anbieters.</span><span class="sxs-lookup"><span data-stu-id="c0cba-117">**From an environment variable**: Credential providers can be stored anywhere and made accessible to `nuget.exe` by setting the `%NUGET_CREDENTIALPROVIDERS_PATH%` environment variable to the provider location.</span></span> <span data-ttu-id="c0cba-118">Diese Variable kann eine durch Semikolons getrennte Liste (z. B. `path1;path2`) Wenn Sie über mehrere Standorte verfügen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-118">This variable can be a semicolon-separated list (for example, `path1;path2`) if you have multiple locations.</span></span>

- <span data-ttu-id="c0cba-119">**Neben nuget.exe**: nuget.exe Anmeldeinformationsanbieter platziert werden können, im gleichen Ordner wie `nuget.exe`.</span><span class="sxs-lookup"><span data-stu-id="c0cba-119">**Alongside nuget.exe**: nuget.exe credential providers can be placed in the same folder as `nuget.exe`.</span></span>

<span data-ttu-id="c0cba-120">Beim Laden der Anmeldeinformationsanbieter, `nuget.exe` sucht die oben genannten Speicherorten nacheinander für jede Datei mit dem Namen `credentialprovider*.exe`, lädt dann die Dateien in der Reihenfolge, die sie aufgeführt sind.</span><span class="sxs-lookup"><span data-stu-id="c0cba-120">When loading credential providers, `nuget.exe` searches the above locations, in order, for any file named `credentialprovider*.exe`, then loads those files in the order they're found.</span></span> <span data-ttu-id="c0cba-121">Wenn mehrere Anmeldeinformationsanbieter im selben Ordner vorhanden sind, sind in alphabetischer Reihenfolge geladen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-121">If multiple credential providers exist in the same folder, they're loaded in alphabetical order.</span></span>

## <a name="creating-a-nugetexe-credential-provider"></a><span data-ttu-id="c0cba-122">Einen Anmeldeinformationsanbieter nuget.exe erstellen</span><span class="sxs-lookup"><span data-stu-id="c0cba-122">Creating a nuget.exe credential provider</span></span>

<span data-ttu-id="c0cba-123">Ein Anmeldeinformationsanbieter ist eine ausführbare Befehlszeilendatei Namens in der Form `CredentialProvider*.exe`, sammelt die Eingaben, erhält die Anmeldeinformationen nach Bedarf und gibt dann den entsprechenden Exit-Statuscode und die Standardausgabe zurück.</span><span class="sxs-lookup"><span data-stu-id="c0cba-123">A credential provider is a command-line executable, named in the form `CredentialProvider*.exe`, that gathers inputs, acquires credentials as appropriate, and then returns the appropriate exit status code and standard output.</span></span>

<span data-ttu-id="c0cba-124">Ein Anbieter muss folgende Anforderungen erfüllen:</span><span class="sxs-lookup"><span data-stu-id="c0cba-124">A provider must do the following:</span></span>

- <span data-ttu-id="c0cba-125">Bestimmen Sie, ob sie Anmeldeinformationen für den Ziel-URI bereitstellen kann, vor dem Initiieren der Übernahme von Anmeldeinformationen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-125">Determine whether it can provide credentials for the targeted URI before initiating credential acquisition.</span></span> <span data-ttu-id="c0cba-126">Wenn dies nicht der Fall ist, sollte der Statuscode 1 ohne Anmeldeinformationen zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="c0cba-126">If not, it should return status code 1 with no credentials.</span></span>
- <span data-ttu-id="c0cba-127">Ändern Sie nicht `Nuget.Config` (z. B. das Festlegen von Anmeldeinformationen vorhanden).</span><span class="sxs-lookup"><span data-stu-id="c0cba-127">Not modify `Nuget.Config` (such as setting credentials there).</span></span>
- <span data-ttu-id="c0cba-128">Handle HTTP-Proxykonfiguration auf eine eigene, als NuGet-bietet keine Proxyinformationen des Plug-Ins.</span><span class="sxs-lookup"><span data-stu-id="c0cba-128">Handle HTTP proxy configuration on its own, as NuGet does not provide proxy information to the plugin.</span></span>
- <span data-ttu-id="c0cba-129">Zurückgeben von Anmeldeinformationen oder die Fehlerdetails, um `nuget.exe` durch Schreiben von JSON-Antwort-Objekt (siehe unten) in "stdout" mit UTF-8-Codierung.</span><span class="sxs-lookup"><span data-stu-id="c0cba-129">Return credentials or error details to `nuget.exe` by writing a JSON response object (see below) to stdout, using UTF-8 encoding.</span></span>
- <span data-ttu-id="c0cba-130">Optional Geben Sie zusätzliche ablaufverfolgungsprotokollierung an Stderr.</span><span class="sxs-lookup"><span data-stu-id="c0cba-130">Optionally emit additional trace logging to stderr.</span></span> <span data-ttu-id="c0cba-131">Keine geheimen Schlüssel sollten in "stderr", jemals geschrieben werden, da am ausführlichkeitsebenen "normal" oder "detailliert" solchen ablaufverfolgungen von NuGet auf der Konsole ausgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="c0cba-131">No secrets should ever be written to stderr, since at verbosity levels "normal" or "detailed" such traces are echoed by NuGet to the console.</span></span>
- <span data-ttu-id="c0cba-132">Unerwarteter Parameter sollten bereitstellen Aufwärtskompatibilität mit zukünftigen Versionen von NuGet ignoriert werden.</span><span class="sxs-lookup"><span data-stu-id="c0cba-132">Unexpected parameters should be ignored, providing forward compatibility with future versions of NuGet.</span></span>

### <a name="input-parameters"></a><span data-ttu-id="c0cba-133">Eingabeparameter</span><span class="sxs-lookup"><span data-stu-id="c0cba-133">Input parameters</span></span>

| <span data-ttu-id="c0cba-134">Parameter-Switch</span><span class="sxs-lookup"><span data-stu-id="c0cba-134">Parameter/Switch</span></span> |<span data-ttu-id="c0cba-135">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="c0cba-135">Description</span></span>|
|----------------|-----------|
| <span data-ttu-id="c0cba-136">URI {Value}</span><span class="sxs-lookup"><span data-stu-id="c0cba-136">Uri {value}</span></span> | <span data-ttu-id="c0cba-137">Die Paket-URI erfordert Anmeldeinformationen für die Datenquelle.</span><span class="sxs-lookup"><span data-stu-id="c0cba-137">The package source URI requiring credentials.</span></span>|
| <span data-ttu-id="c0cba-138">NonInteractive</span><span class="sxs-lookup"><span data-stu-id="c0cba-138">NonInteractive</span></span> | <span data-ttu-id="c0cba-139">Falls vorhanden, gibt Anbieter keine interaktive eingabeaufforderungen aus.</span><span class="sxs-lookup"><span data-stu-id="c0cba-139">If present, provider does not issue interactive prompts.</span></span> |
| <span data-ttu-id="c0cba-140">IsRetry</span><span class="sxs-lookup"><span data-stu-id="c0cba-140">IsRetry</span></span> | <span data-ttu-id="c0cba-141">Falls vorhanden, gibt an, dass dieser Versuch eine Wiederholung für einen zuvor fehlgeschlagenen Versuch ist.</span><span class="sxs-lookup"><span data-stu-id="c0cba-141">If present, indicates that this attempt is a retry of a previously failed attempt.</span></span> <span data-ttu-id="c0cba-142">Anbieter verwenden Sie dieses Flag in der Regel um sicherzustellen, dass sie alle vorhandenen Caches zu umgehen und nach Möglichkeit zum Eingeben neuer Anmeldeinformationen aufgefordert.</span><span class="sxs-lookup"><span data-stu-id="c0cba-142">Providers typically use this flag to ensure that they bypass any existing cache and prompt for new credentials if possible.</span></span>|
| <span data-ttu-id="c0cba-143">Ausführlichkeit {Value}</span><span class="sxs-lookup"><span data-stu-id="c0cba-143">Verbosity {value}</span></span> | <span data-ttu-id="c0cba-144">Falls vorhanden, einen der folgenden Werte: "normale", "quiet" oder "detailliert".</span><span class="sxs-lookup"><span data-stu-id="c0cba-144">If present, one of the following values: "normal", "quiet", or "detailed".</span></span> <span data-ttu-id="c0cba-145">Wenn kein Wert angegeben wird, wird standardmäßig in "Normal".</span><span class="sxs-lookup"><span data-stu-id="c0cba-145">If no value is supplied, defaults to "normal".</span></span> <span data-ttu-id="c0cba-146">Anbieter sollten diese ein Hinweis auf die Ebene der optionalen Protokollierung verwenden, um den Standardfehlerstream auszugeben.</span><span class="sxs-lookup"><span data-stu-id="c0cba-146">Providers should use this as an indication of the level of optional logging to emit to the standard error stream.</span></span> |

### <a name="exit-codes"></a><span data-ttu-id="c0cba-147">Exitcodes</span><span class="sxs-lookup"><span data-stu-id="c0cba-147">Exit codes</span></span>

| <span data-ttu-id="c0cba-148">Code</span><span class="sxs-lookup"><span data-stu-id="c0cba-148">Code</span></span> |<span data-ttu-id="c0cba-149">Ergebnis</span><span class="sxs-lookup"><span data-stu-id="c0cba-149">Result</span></span> | <span data-ttu-id="c0cba-150">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="c0cba-150">Description</span></span> |
|----------------|-----------|-----------|
| <span data-ttu-id="c0cba-151">0</span><span class="sxs-lookup"><span data-stu-id="c0cba-151">0</span></span> | <span data-ttu-id="c0cba-152">Erfolgreich</span><span class="sxs-lookup"><span data-stu-id="c0cba-152">Success</span></span> | <span data-ttu-id="c0cba-153">Die Anmeldeinformationen wurden erfolgreich abgerufen und in "stdout" geschrieben wurden.</span><span class="sxs-lookup"><span data-stu-id="c0cba-153">Credentials were successfully acquired and have been written to stdout.</span></span>|
| <span data-ttu-id="c0cba-154">1</span><span class="sxs-lookup"><span data-stu-id="c0cba-154">1</span></span> | <span data-ttu-id="c0cba-155">ProviderNotApplicable</span><span class="sxs-lookup"><span data-stu-id="c0cba-155">ProviderNotApplicable</span></span> | <span data-ttu-id="c0cba-156">Der aktuelle Anbieter bietet keine Anmeldeinformationen für den angegebenen URI.</span><span class="sxs-lookup"><span data-stu-id="c0cba-156">The current provider does not provide credentials for the given URI.</span></span>|
| <span data-ttu-id="c0cba-157">2</span><span class="sxs-lookup"><span data-stu-id="c0cba-157">2</span></span> | <span data-ttu-id="c0cba-158">Fehler</span><span class="sxs-lookup"><span data-stu-id="c0cba-158">Failure</span></span> | <span data-ttu-id="c0cba-159">Der Anbieter ist der richtige Anbieter für den angegebenen URI, jedoch kann keine Anmeldeinformationen bereitstellen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-159">The provider is the correct provider for the given URI, but cannot provide credentials.</span></span> <span data-ttu-id="c0cba-160">In diesem Fall nuget.exe unternimmt keine Authentifizierung und schlägt fehl.</span><span class="sxs-lookup"><span data-stu-id="c0cba-160">In this case, nuget.exe will not retry authentication and will fail.</span></span> <span data-ttu-id="c0cba-161">Ein typisches Beispiel ist, wenn ein Benutzer eine interaktive Anmeldung abgebrochen wird.</span><span class="sxs-lookup"><span data-stu-id="c0cba-161">A typical example is when a user cancels an interactive login.</span></span> |

### <a name="standard-output"></a><span data-ttu-id="c0cba-162">Standardausgabe</span><span class="sxs-lookup"><span data-stu-id="c0cba-162">Standard output</span></span>

| <span data-ttu-id="c0cba-163">Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="c0cba-163">Property</span></span> |<span data-ttu-id="c0cba-164">Hinweise</span><span class="sxs-lookup"><span data-stu-id="c0cba-164">Notes</span></span>|
|----------------|-----------|
| <span data-ttu-id="c0cba-165">Benutzername</span><span class="sxs-lookup"><span data-stu-id="c0cba-165">Username</span></span> | <span data-ttu-id="c0cba-166">Der Benutzername für authentifizierte Anforderungen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-166">Username for authenticated requests.</span></span>|
| <span data-ttu-id="c0cba-167">Kennwort</span><span class="sxs-lookup"><span data-stu-id="c0cba-167">Password</span></span> | <span data-ttu-id="c0cba-168">Kennwort für authentifizierte Anforderungen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-168">Password for authenticated requests.</span></span>|
| <span data-ttu-id="c0cba-169">Meldung</span><span class="sxs-lookup"><span data-stu-id="c0cba-169">Message</span></span> | <span data-ttu-id="c0cba-170">Optionale Details der Antwort, die nur verwendet, um weitere Details in möglichen Schwachstellen angezeigt.</span><span class="sxs-lookup"><span data-stu-id="c0cba-170">Optional details about the response, used only to show additional details in failure cases.</span></span> |

<span data-ttu-id="c0cba-171">Beispiel für "stdout":</span><span class="sxs-lookup"><span data-stu-id="c0cba-171">Example stdout:</span></span>

    { "Username" : "freddy@example.com",
      "Password" : "bwm3bcx6txhprzmxhl2x63mdsul6grctazoomtdb6kfbof7m3a3z",
      "Message"  : "" }

## <a name="troubleshooting-a-credential-provider"></a><span data-ttu-id="c0cba-172">Problembehandlung bei einer Anmeldeinformationsanbieter</span><span class="sxs-lookup"><span data-stu-id="c0cba-172">Troubleshooting a credential provider</span></span>

<span data-ttu-id="c0cba-173">Zu diesem Zeitpunkt angeben nicht NuGet viel direkte Unterstützung, für das Debuggen von benutzerdefinierten Anmeldeinformationsanbieter; [ausstellen 4598](https://github.com/NuGet/Home/issues/4598) diese Arbeit nachverfolgt.</span><span class="sxs-lookup"><span data-stu-id="c0cba-173">At present, NuGet doesn't provide much direct support for debugging custom credential providers; [issue 4598](https://github.com/NuGet/Home/issues/4598) is tracking this work.</span></span>

<span data-ttu-id="c0cba-174">Sie können auch die folgenden Aktionen ausführen:</span><span class="sxs-lookup"><span data-stu-id="c0cba-174">You can also do the following:</span></span>

- <span data-ttu-id="c0cba-175">Führen Sie nuget.exe mit der `-verbosity` Switch, ausführliche Ausgabe zu überprüfen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-175">Run nuget.exe with the `-verbosity` switch to inspect detailed output.</span></span>
- <span data-ttu-id="c0cba-176">Hinzufügen von Debugmeldungen an `stdout` in den entsprechenden Stellen.</span><span class="sxs-lookup"><span data-stu-id="c0cba-176">Add debug messages to `stdout` in appropriate places.</span></span>
- <span data-ttu-id="c0cba-177">Achten Sie darauf, dass Sie nuget.exe 3.3 oder höher verwenden.</span><span class="sxs-lookup"><span data-stu-id="c0cba-177">Be sure that you're using nuget.exe 3.3 or higher.</span></span>
- <span data-ttu-id="c0cba-178">Fügen Sie die Debugger beim Start durch diesen Codeausschnitt:</span><span class="sxs-lookup"><span data-stu-id="c0cba-178">Attach debugger on startup with this code snippet:</span></span>

    ```cs
    while (!Debugger.IsAttached)
    {
        System.Threading.Thread.Sleep(100);
    }
    Debugger.Break();
    ```

<span data-ttu-id="c0cba-179">Weitere Hilfe benötigen [senden Sie eine Supportanfrage ein nuget.org](https://www.nuget.org/policies/Contact).</span><span class="sxs-lookup"><span data-stu-id="c0cba-179">For further help, [submit a support request a nuget.org](https://www.nuget.org/policies/Contact).</span></span>
